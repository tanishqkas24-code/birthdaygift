<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Happy Birthday, My Love!</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Poppins:wght@300;400&display=swap');

        :root {
            --primary-color: #c7a17a;
            --secondary-color: #f0e6e0;
            --background-color: #1a1a1a;
            --text-color: #e0e0e0;
            --accent-color: #ff5e62;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--background-color);
            color: var(--text-color);
            overflow-x: hidden;
            scroll-behavior: smooth;
        }

        .container {
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
        }

        /* Hero Section */
        .hero {
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            position: relative;
            background: linear-gradient(rgba(0,0,0,0.7), rgba(0,0,0,0.7)), url('https://images.unsplash.com/photo-1531602283577-1e5f824d5b6a?ixlib=rb-4.0.3&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=1770&q=80') no-repeat center center/cover;
            animation: fadeIn 2s ease-in-out;
        }

        .hero-title {
            font-family: 'Playfair Display', serif;
            font-size: 5rem;
            color: var(--primary-color);
            margin-bottom: 1rem;
            animation: slideInFromTop 1.5s ease-out;
        }

        .hero-subtitle {
            font-size: 1.5rem;
            color: var(--secondary-color);
            margin-bottom: 2rem;
            animation: slideInFromBottom 1.5s ease-out;
        }

        .scroll-down {
            position: absolute;
            bottom: 2rem;
            font-size: 1rem;
            color: var(--secondary-color);
            animation: bounce 2s infinite;
        }

        /* Journey Section */
        .journey-section {
            padding: 6rem 0;
            opacity: 0;
            transform: translateY(50px);
            transition: opacity 1s, transform 1s;
        }

        .journey-section.visible {
            opacity: 1;
            transform: translateY(0);
        }

        .section-title {
            font-family: 'Playfair Display', serif;
            font-size: 3rem;
            text-align: center;
            margin-bottom: 3rem;
            color: var(--primary-color);
        }

        .memory-gallery {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
        }

        .memory-card {
            background: #2a2a2a;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 10px 20px rgba(0,0,0,0.3);
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .memory-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 30px rgba(0,0,0,0.5);
        }

        .memory-card img {
            width: 100%;
            height: 200px;
            object-fit: cover;
        }

        .memory-card-content {
            padding: 1.5rem;
        }

        .memory-card-title {
            font-size: 1.5rem;
            color: var(--primary-color);
            margin-bottom: 0.5rem;
        }

        /* Wishes Section */
        .wishes-section {
            background-color: #2a2a2a;
        }

        .wish {
            font-size: 1.2rem;
            line-height: 1.8;
            margin-bottom: 2rem;
            text-align: center;
            max-width: 800px;
            margin-left: auto;
            margin-right: auto;
        }

        /* Final Message Section */
        .final-message {
            text-align: center;
        }

        .heart {
            font-size: 4rem;
            color: var(--accent-color);
            animation: beat 1.5s infinite;
        }

        /* Animations */
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        @keyframes slideInFromTop {
            from { transform: translateY(-50px); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }

        @keyframes slideInFromBottom {
            from { transform: translateY(50px); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }

        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% {
                transform: translateY(0);
            }
            40% {
                transform: translateY(-15px);
            }
            60% {
                transform: translateY(-7px);
            }
        }

        @keyframes beat {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.1); }
        }

    </style>
</head>
<body>

    <section class="hero">
        <canvas id="stars"></canvas>
        <div class="container">
            <h1 class="hero-title">Happy Birthday, [Your Girlfriend's Name]</h1>
            <p class="hero-subtitle">My Dearest Love, My Everything</p>
        </div>
        <div class="scroll-down">
            <p>Scroll to begin our journey</p>
            <span>&darr;</span>
        </div>
    </section>

    <section id="our-journey" class="journey-section">
        <div class="container">
            <h2 class="section-title">A Journey Through Our Memories</h2>
            <div class="memory-gallery">
                <div class="memory-card">
                    <img src="https://images.unsplash.com/photo-1502318217862-8c29694d301a?ixlib=rb-4.0.3&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=1770&q=80" alt="Memory 1">
                    <div class="memory-card-content">
                        <h3 class="memory-card-title">The Day We Met</h3>
                        <p>Customize this with a short description of your memory.</p>
                    </div>
                </div>
                <div class="memory-card">
                    <img src="https://images.unsplash.com/photo-1519681393784-d120267933ba?ixlib=rb-4.0.3&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=1770&q=80" alt="Memory 2">
                    <div class="memory-card-content">
                        <h3 class="memory-card-title">Our First Adventure</h3>
                        <p>Customize this with another beautiful memory.</p>
                    </div>
                </div>
                <div class="memory-card">
                    <img src="https://images.unsplash.com/photo-1532274402911-5a369e4c4bb5?ixlib=rb-4.0.3&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=1770&q=80" alt="Memory 3">
                    <div class="memory-card-content">
                        <h3 class="memory-card-title">That Special Moment</h3>
                        <p>Add a third memory that you both cherish.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section id="my-wishes" class="journey-section wishes-section">
        <div class="container">
            <h2 class="section-title">My Wishes For You</h2>
            <p class="wish">
                On your special day, I wish for all your dreams to take flight. May your heart always be filled with the same joy and warmth you bring into my life.
            </p>
            <p class="wish">
                I wish you endless laughter, success in all you do, and a future that is as bright and beautiful as your smile.
            </p>
        </div>
    </section>

    <section id="final-message" class="journey-section">
        <div class="container final-message">
            <h2 class="section-title">Forever and Always</h2>
            <p class="wish">
                You are the most incredible person I have ever known. Thank you for being you. Happy Birthday, my love.
            </p>
            <div class="heart">&hearts;</div>
        </div>
    </section>


    <script>
        // Starry background effect
        const canvas = document.getElementById('stars');
        const ctx = canvas.getContext('2d');

        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;

        let stars = [];

        for (let i = 0; i < 200; i++) {
            stars.push({
                x: Math.random() * canvas.width,
                y: Math.random() * canvas.height,
                radius: Math.random() * 1.5,
                alpha: Math.random(),
                speed: Math.random() * 0.5 + 0.1
            });
        }

        function drawStars() {
            ctx.clearRect(0, 0, canvas.width, canvas.height);
            stars.forEach(star => {
                ctx.beginPath();
                ctx.arc(star.x, star.y, star.radius, 0, Math.PI * 2);
                ctx.fillStyle = `rgba(255, 255, 255, ${star.alpha})`;
                ctx.fill();
            });
        }

        function updateStars() {
            stars.forEach(star => {
                star.y -= star.speed;
                if (star.y < 0) {
                    star.y = canvas.height;
                }
            });
        }

        function animate() {
            drawStars();
            updateStars();
            requestAnimationFrame(animate);
        }

        animate();

        // Scroll reveal animation
        const sections = document.querySelectorAll('.journey-section');

        const observer = new IntersectionObserver(entries => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, { threshold: 0.1 });

        sections.forEach(section => {
            observer.observe(section);
        });

    </script>
</body>
</html>
